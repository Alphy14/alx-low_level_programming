#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 * This program makes use of the puts () function
 * it prints out the content of the variable Mywork
 */
int main(void)
{
	char Mywork[] = "\"Programming is like building a multilingual puzzle";

	puts(Mywork);
	return (0);
}
